class Augmentation {
    constructor(
        readonly id: string,
        readonly name: string,
        readonly category: string,
        readonly quantity: number = 0
    ) {
    }
}

export default Augmentation;